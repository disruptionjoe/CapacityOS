# 03 - Runtime Active

Use this bundle for current live state that should normalize into the next
runtime.

Examples:

- active tasks
- active plans
- current queue inputs
- latest logs
- latest summaries

This is normalization-heavy material, not direct carry-over canon.
